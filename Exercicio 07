Efetuar o cálculo e a apresentação do valor de uma prestação em atraso, utilizando a fórmula, PRESTAÇÃO = VALOR + (VALOR x (TAXA / 100) x TEMPO).

Function Main ()
 
    local nPre := 0
    local nVal := 0
    local nTaxa := 0
    local nTemp := 0
 
    ? "Exercicio 7"
    ? " "
 
    ACCEPT "Digite o valor em atraso: " TO nVal
    ACCEPT "Digite o valor da Taxa: " TO nTaxa
    ACCEPT "Digite o tempo em atraso: " TO nTemp
 
    nPre := (Val(nVal) + (Val(nTaxa) / 100) * Val(nTemp))
 
    QOUT (" ")
    QOUT ("O valor da prestacao atrasada ficou: " , nPre )
    QOUT (" ")
 
    RETURN nil
