Elaborar um programa que exiba na tela automaticamente todos os números naturais entre 0 e 100,
em ordem crescente. Fazer 3 versões desta solução: uma utilizando laço com pré-teste (enquanto),
outra usando laço com pós-teste (repita) e uma última usando laço finito (para).

Function Main()
 
    local n1 := 0
    local n2 := 0
 
    For n1 := 0 TO 100
         QOUT (n1)
 
    next n1
 
    While n2 <= 100
         QOUT(n2)
           n2 ++
     Enddo
 
RETURN nil
