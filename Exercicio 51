Elaborar um programa que exiba na tela automaticamente todos os múltiplos de 3 existentes entre 1 e 100.
Utilizar obrigatoriamente o laço finito (para) para esta solução. 

Function Main()
 
    local nN := 0
 
    ? "Os multiplos de 3 de 1 a 100 sao:"  
    for nN := 1 TO 100
        if (nN % 3 == 0)
            QOUT (nN)
        endif
    next
 
RETURN nil
