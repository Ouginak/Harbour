Elaborar um programa que efetue a leitura de três valores. (A, B e C) e apresente como resultado final o quadrado da soma dos três valores lidos.

Function Main()
 
    local nA := 0
    local nB := 0
    local nC := 0
    local nResu := 0
 
    ? "Exercicio 15"
    ? " "
 
    ACCEPT "Digite um valor para A: " TO nA
    ACCEPT "Digite um valor para B: " TO nB
    ACCEPT "Digite um valor para C: " TO nC
 
    nResu := (((Val(nA) + Val(nB) + Val(nC)) ^ 2 ))
 
    QOUT ("O resultado da soma ao quadrado de" , nA , "," , nB , "," , nC , "e: " + Alltrim(Str(nResu)))
 
 RETURN nil
