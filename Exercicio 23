Desenvolva um programa que dado um número, calcule e exiba se ele é par ou ímpar.

Function Main()
 
    local n1 := 0
 
    ACCEPT "Digite um numero: " TO n1
 
    if (val(n1) % 2 == 0)
        QOUT ("O numero" , n1 , "e par")
    Else
        QOUT ("O numero" , n1 , "e impar")
    endif
 
 RETURN nil
